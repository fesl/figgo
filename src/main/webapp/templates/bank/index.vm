#set( $title = "Figgo - $!{domain.Name} - Banco" )
#set( $currentModule = "BANK" )
#parse( "templates/layout_intern.vm" )

#head()

#fullHeaderDomain()

#@body()
<div class="left">
    <section id="about-intern">
        <h2>$domain.Name</h2>
        #if( $domain.AvatarKey )
        <a href="/domain/upload"><img class="avatar" src="/blob/$domain.AvatarKey" alt="Avatar do domínio" /></a>
        #else
        <a href="/domain/upload"><img class="avatar" src="http://lorempixum.com/180/180/" alt="Avatar do domínio" /></a>
        #end
        <ul class="links">
            <li><a href="#">Home</a></li>
            <li><a href="#">Sobre</a></li>
            #foreach( $module in $domain.ModulesEnabled )
                <li #if( $module == $currentModule )class="selected"#end><a href="/$module.toLowerCase()">$module</a></li>
            #end
        </ul>
    </section>
</div>
<div class="left">
    <section id="main-intern">
        <h3>Acesso rápido</h3>
        <nav>
            <ul class="bank-shortcuts">
                <li><a href="/bank">Minha conta</a></li>
                <li><a href="/bank/transfer">Trasferência</a></li>
                <li><a href="/bank/statement">Extrato</a></li>
                <li><a href="/bank/other">Outras consultas</a></li>
            </ul>
        </nav>

        <hr />

        <h3>Minha conta</h3>
        <table>
            <thead>
                <tr>
                    <th>Data</th>
                    <th>Destino</th>
                    <th>Comentário</th>
                    <th>Valor</th>
                </tr>
            </thead>
            <tfoot>
                <td colspan="3" class="balance">SALDO</td>
                <td class=#if( $balance >= 0 )"blue"#else"red"#end>M$ $formatter. format($balance)</td>
            </tfoot>
            <tbody>
            #if( $transactions.size() > 0 )
                #foreach( $transaction in $transactions )
                <tr>
                    <td>$formatter.format($transaction.Date)</td>
                    <td>$transaction.AccountDest</td>
                    <td>$transaction.Comment</td>
                    <td class=#if( $transaction.AccountDest == $user.Email )"blue">#else"red">-#end$formatter.format($transaction.Amount)</td>
                </tr>
                #end
            #else
                <tr><td colspan="4">Nenhuma transação até o momento.</td></tr>
            #end
            </tbody>
        </table>
    </section>
</div>
#end
    
#scripts()