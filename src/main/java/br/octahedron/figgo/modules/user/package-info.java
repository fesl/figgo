/**
 * This package assumes that no namespace is configured. It's an global module.
 * 
 * TODO fazer a fachada deste pacote alterar o namespace para o default antes de realizar qualquer operação
 * e voltar pra o valor inicial após qualquer operação, já que a fachada poderá ser usada também para comunicação interna.
 */
package br.octahedron.figgo.modules.user;